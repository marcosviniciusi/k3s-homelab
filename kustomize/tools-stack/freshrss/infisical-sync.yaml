apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: freshrss-infisical-secret
spec:
  resyncInterval: 60
  hostAPI: "https://cofre.vinicima.com/api"

  authentication:
    serviceToken:
      serviceTokenSecretReference:
        secretName: freshrss-service-token
        secretNamespace: tools
      secretsScope:
        envSlug: production
        secretsPath: /freshrss

  managedKubeSecretReferences:
    - secretName: freshrss-secrets
      secretNamespace: tools
      secretType: Opaque
      template:
        includeAllSecrets: true



