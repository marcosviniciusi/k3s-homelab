apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: seafile-infisical-secret
spec:
  resyncInterval: 60
  hostAPI: "https://cofre.vinicima.com/api"

  authentication:
    serviceToken:
      serviceTokenSecretReference:
        secretName: seafile-service-token
        secretNamespace: tools
      secretsScope:
        envSlug: production
        secretsPath: /seafile

  managedKubeSecretReferences:
    - secretName: seafile-secrets
      secretNamespace: tools
      secretType: Opaque
      template:
        includeAllSecrets: true



