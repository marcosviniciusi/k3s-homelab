apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: radarra-infisical-secret
spec:
  resyncInterval: 60
  hostAPI: "https://cofre.vinicima.com/api"

  authentication:
    serviceToken:
      serviceTokenSecretReference:
        secretName: radarra-service-token
        secretNamespace: tools
      secretsScope:
        envSlug: production
        secretsPath: /radarr-animes

  managedKubeSecretReferences:
    - secretName: radarrasecrets
      secretNamespace: tools
      secretType: Opaque
      template:
        includeAllSecrets: true



