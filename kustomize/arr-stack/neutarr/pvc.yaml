apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: neutarr-config
  labels:
    app.kubernetes.io/name: neutarr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi