apiVersion: v1
kind: Service
metadata:
  name: blockbusterr
  labels:
    app: blockbusterr
spec:
  externalTrafficPolicy: Local
  loadBalancerIP: 192.168.253.39
  type: LoadBalancer
  selector:
    app: blockbusterr
  ports:
    - name: http
      port: 9090
      targetPort: 9090
      protocol: TCP