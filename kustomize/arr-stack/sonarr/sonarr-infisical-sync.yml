apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: sonarr-infisical-secret
spec:
  resyncInterval: 60
  hostAPI: "https://cofre.vinicima.com/api"

  authentication:
    serviceToken:
      serviceTokenSecretReference:
        secretName: sonarr-service-token
        secretNamespace: arr
      secretsScope:
        envSlug: production
        secretsPath: /sonarr

  managedKubeSecretReferences:
    - secretName: sonarr-secrets
      secretNamespace: arr
      secretType: Opaque
      template:
        includeAllSecrets: true



