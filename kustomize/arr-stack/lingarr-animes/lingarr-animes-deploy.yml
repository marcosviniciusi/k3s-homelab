kind: Deployment
apiVersion: apps/v1
metadata:
  name: lingarr-animes
  labels:
    app: lingarr-animes
  annotations:
    keel.sh/policy: force
    keel.sh/trigger: poll
    keel.sh/pollSchedule: "@hourly"
    keel.sh/match-tag: "true"
spec:
  revisionHistoryLimit: 0
  strategy:
    type: Recreate
  replicas: 1
  selector:
    matchLabels:
      app: lingarr-animes
  template:
    metadata:
      labels:
        app: lingarr-animes
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/hostname
                operator: In
                values:
                - aloy
                - ellie
                - joel
      containers:
      - name: lingarr-animes
        image: lingarr/lingarr:latest
        resources:
          limits:
            cpu: "2"
            memory: "4Gi"
        ports:
        - name: lingarr-animes
          containerPort: 9876
        envFrom:
        - secretRef:
            name: lingarr-animes-secrets
        livenessProbe:
          httpGet:
            path: /
            port: 9876
          initialDelaySeconds: 30
          periodSeconds: 30
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /
            port: 9876
          initialDelaySeconds: 10
          periodSeconds: 15
        volumeMounts:
        - name: lingarr-animes
          mountPath: /app/config
        - name: nfs-animes-movies
          mountPath: /data/videos/animes-movies
        - name: nfs-animes-series
          mountPath: /data/videos/animes
      volumes:
      - name: lingarr-animes
        persistentVolumeClaim:
          claimName: lingarr-animes
      - name: nfs-animes-movies
        nfs:
          server: 192.168.253.250
          path: /export/data/videos/animes-movies
      - name: nfs-animes-series
        nfs:
          server: 192.168.253.250
          path: /export/data2/videos/animes

