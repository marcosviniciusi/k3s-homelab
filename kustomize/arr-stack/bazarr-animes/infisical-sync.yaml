apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: bazarr-animes-infisical-secret
spec:
  resyncInterval: 60
  hostAPI: "https://cofre.vinicima.com/api"

  authentication:
    serviceToken:
      serviceTokenSecretReference:
        secretName: bazarr-animes-service-token
        secretNamespace: arr
      secretsScope:
        envSlug: production
        secretsPath: /bazarr-animes

  managedKubeSecretReferences:
    - secretName: bazarr-animes-secrets
      secretNamespace: arr
      secretType: Opaque
      template:
        includeAllSecrets: true



