apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: autobrr-infisical-secret
spec:
  resyncInterval: 60
  hostAPI: "https://cofre.vinicima.com/api"

  authentication:
    serviceToken:
      serviceTokenSecretReference:
        secretName: autobrr-service-token
        secretNamespace: arr
      secretsScope:
        envSlug: production
        secretsPath: /autobrr

  managedKubeSecretReferences:
    - secretName: autobrr-secrets
      secretNamespace: arr
      secretType: Opaque
      template:
        includeAllSecrets: true



