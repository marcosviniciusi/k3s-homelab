apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: readarr-infisical-secret
spec:
  resyncInterval: 60
  hostAPI: "https://cofre.vinicima.com/api"

  authentication:
    serviceToken:
      serviceTokenSecretReference:
        secretName: readarr-service-token
        secretNamespace: arr
      secretsScope:
        envSlug: production
        secretsPath: /readarr

  managedKubeSecretReferences:
    - secretName: readarr-secrets
      secretNamespace: arr
      secretType: Opaque
      template:
        includeAllSecrets: true



