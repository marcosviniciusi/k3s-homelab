apiVersion: v1
data:
  amigosshare-trashguides-ptbr.yml: |-
    ---
    id: amigosshare-trashguides-pt-br
    name: Amigos Share Club TrashGuides PT-BR
    description: "Amigos Share Club (ASC) is a BRAZILIAN Private Torrent Tracker for MOVIES / TV / GENERAL"
    language: pt-BR
    type: private
    encoding: UTF-8
    requestDelay: 2
    links:
      - https://cliente.amigos-share.club/
    legacylinks:
      - http://amigos-share.club/
      - https://amigos-share.club/

    caps:
      categorymappings:
        - {id: 75, cat: XXX, desc: "Adultos: Hentai"}
        - {id: 107, cat: XXX, desc: "Adultos: Gay"}
        - {id: 108, cat: XXX, desc: "Adultos: Transex/Female"}
        - {id: 74, cat: XXX, desc: "Adultos: XXX"}
        - {id: 146, cat: XXX, desc: "Adultos: OnlyFans"}
        - {id: 69, cat: TV/Anime, desc: "Anime: Anime"}
        - {id: 116, cat: TV/Anime, desc: "Anime: Filmes"}
        - {id: 118, cat: TV/Anime, desc: "Anime: Séries"}
        - {id: 21, cat: PC/0day, desc: "Aplicativos: Windows"}
        - {id: 28, cat: PC/Mobile-iOS, desc: "Aplicativos: iPod"}
        - {id: 27, cat: PC/Mobile-iOS, desc: "Aplicativos: iPad"}
        - {id: 26, cat: PC/Mobile-iOS, desc: "Aplicativos: iPhone"}
        - {id: 25, cat: PC, desc: "Aplicativos: Linux"}
        - {id: 23, cat: PC/Mobile-Android, desc: "Aplicativos: Android"}
        - {id: 22, cat: PC/Mac, desc: "Aplicativos: Mac"}
        - {id: 73, cat: Other, desc: "Cursos: Video Aula"}
        - {id: 72, cat: Other, desc: "Cursos: Apostila"}
        - {id: 71, cat: Other, desc: "Cursos: Cursos"}
        - {id: 67, cat: Books/EBook, desc: "Ebook: Livros"}
        - {id: 112, cat: Books/Comics, desc: "Ebook: HQs"}
        - {id: 147, cat: Books/Comics, desc: "Ebook: Mangas"}
        - {id: 121, cat: Audio/Audiobook, desc: "Ebook: Audio-book"}
        - {id: 68, cat: Books/Mags, desc: "Ebook: Revistas"}
        - {id: 119, cat: Movies, desc: "Filmes:"}
        - {id: 70, cat: XXX, desc: "Fotos: Revistas, Livros e Fotos"}
        - {id: 51, cat: Console/PS3, desc: "Jogos: Ps3"}
        - {id: 54, cat: Console/XBox 360, desc: "Jogos: Xbox360"}
        - {id: 109, cat: Console, desc: "Jogos: Emulação"}
        - {id: 110, cat: Console, desc: "Jogos: Nintendo Switch"}
        - {id: 52, cat: Console, desc: "Jogos: Dreamcast"}
        - {id: 50, cat: Console, desc: "Jogos: Ps2"}
        - {id: 49, cat: Console, desc: "Jogos: Ps1"}
        - {id: 48, cat: PC/Mac, desc: "Jogos: Mac"}
        - {id: 55, cat: Console/Wii, desc: "Jogos: Wii"}
        - {id: 82, cat: Console/PSP, desc: "Jogos: PSP"}
        - {id: 47, cat: PC/Games, desc: "Jogos: Pc"}
        - {id: 61, cat: Console, desc: "Jogos: Emuladores e Roms"}
        - {id: 58, cat: Console/NDS, desc: "Jogos: Nintendo DS"}
        - {id: 57, cat: Console, desc: "Jogos: Android"}
        - {id: 56, cat: Console/XBox, desc: "Jogos: Xbox"}
        - {id: 78, cat: Console/XBox One, desc: "Jogos: Xbox One"}
        - {id: 79, cat: Console/PS4, desc: "Jogos: Ps4"}
        - {id: 129, cat: Other, desc: "Outros: Fonts"}
        - {id: 128, cat: Other, desc: "Outros: Scripts"}
        - {id: 127, cat: Other, desc: "Outros: Add-ons"}
        - {id: 130, cat: Other, desc: "Outros: MOD"}
        - {id: 125, cat: Other, desc: "Outros: Pack de Imagens"}
        - {id: 124, cat: Other, desc: "Outros: Vetores"}
        - {id: 122, cat: Other, desc: "Outros: Plug-ins"}
        - {id: 126, cat: Other, desc: "Outros: Extensões"}
        - {id: 120, cat: TV, desc: "Series:"}
        - {id: 64, cat: TV, desc: "Tv: Fechada"}
        - {id: 62, cat: TV/Sport, desc: "Tv: Esportes"}
        - {id: 63, cat: TV, desc: "Tv: Aberta"}

      modes:
        search: [q]
        tv-search: [q, season, ep]
        movie-search: [q]
        music-search: [q]
        book-search: [q]

    settings:
      - name: username
        type: text
        label: Username
      - name: password
        type: password
        label: Password
      - name: info_results
        type: info
        label: "Search results"
        default: "Set <b>Exibição De Torrents</b> to <b>Lista</b> in your <b>Minhas Configurações</b>.<br />Using <b>Capas</b> is not supported and will return 0 results."
      - name: freeleech
        type: checkbox
        label: Filter freeleech only
        default: false
      - name: sort
        type: select
        label: Sort requested from site
        default: id
        options:
          id: created
          seeders: seeders
          size: size
          name: title
      - name: type
        type: select
        label: Order requested from site
        default: desc
        options:
          desc: desc
          asc: asc
      - name: info_activity
        type: info
        label: Account Inactivity
        default: "Inactive accounts, that is, without access for 90 days (3 months), will be automatically deleted by the system; with no possibility of reactivation."

    login:
      path: account-login.php
      method: post
      inputs:
        username: "{{ .Config.username }}"
        password: "{{ .Config.password }}"
        remember: yes
      error:
        - selector: div.alert-error
      test:
        path: torrents-search.php
        selector: a[href="account-logout.php"]

    search:
      paths:
        - path: torrents-search.php
        - path: torrents-search.php
          inputs:
            page: 1
        - path: torrents-search.php
          inputs:
            page: 2
        - path: torrents-search.php
          inputs:
            page: 3
        - path: torrents-search.php
          inputs:
            page: 4
      inputs:
        search: "{{ re_replace .Keywords \"[\\s]+\" \"%\" }}"
        sort: "{{ .Config.sort }}"
        order: "{{ .Config.type }}"
      keywordsfilters:
        - name: re_replace
          args: ["(\\s*\\b((19|20)\\d{2})\\b)", ""]

      rows:
        selector: "div#fancy-list-group ul.list-group li.list-group-item{{ if .Config.freeleech }}:has(span.badge-success:contains(\"FREE\")){{ else }}{{ end }}"

      fields:
        category_default:
          selector: div.list-group-item-addon img
          attribute: src
          optional: true
          case:
            "[src$=\"/filmes2.fw.png\"]": 119
            "[src$=\"/seriados.fw.png\"]": 120
        category:
          selector: a[href*="cat="]
          attribute: href
          optional: true
          default: "{{ .Result.category_default }}"
          filters:
            - name: querystring
              args: cat
        _quality:
          selector: div.list-group-item-content p.m-0 span.badge-primary:contains("1080p"), div.list-group-item-content p.m-0 span.badge-primary:contains("720p"), div.list-group-item-content p.m-0 span.badge-primary:contains("4k")
          optional: true
          filters:
            - name: replace
              args: ["4k", "2160p"]
        _year:
          selector: div.list-group-item-content p.m-0 span.badge-primary[style$="#246AB6;"]
          optional: true
        _type:
          selector: div.list-group-item-content p.m-0 span.badge-info:contains("Rip"), div.list-group-item-content p.m-0 span.badge-info:contains("WEB-"), div.list-group-item-content p.m-0 span.badge-info:contains("TV"), div.list-group-item-content p.m-0 span.badge-info:contains("Blu-Ray"), div.list-group-item-content p.m-0 span.badge-info:contains("BD50"), div.list-group-item-content p.m-0 span.badge-info:contains("MUX"), div.list-group-item-content p.m-0 span.badge-info:contains("DVD"), div.list-group-item-content p.m-0 span.badge-info:contains("320"), div.list-group-item-content p.m-0 span.badge-info:contains("CAM"), div.list-group-item-content p.m-0 span.badge-info:contains("rip")
          optional: true
        _language:
          selector: div.list-group-item-content p.m-0 span.badge-primary[style$="#b6249d;"]
          optional: true
        title:
          selector: a[href*="torrents-details.php?id="], a[href*="details-misc.php?id="]
          filters:
            - name: re_replace
              args: ["^(.*?)[\\(](.*?)[\\)](.*?)$", "$2$3"]
            - name: append
              args: "{{ if .Result._year }} {{ .Result._year }}{{ else }}{{ end }}"
            - name: append
              args: "{{ if .Result._quality }} {{ .Result._quality }}{{ else }}{{ end }}"
            - name: append
              args: "{{ if .Result._type }} {{ .Result._type }}{{ else }}{{ end }}"
            - name: append
              args: "{{ if .Result._language }} {{ .Result._language }}{{ else }}{{ end }}"
            # REMOVIDO: a linha que adicionava "Brazilian" antes
            # - name: re_replace
            #   args: ["(Dual|[Nn]acional|[Dd]ublado)", "Brazilian $1"]
            # SUBSTITUIÇÕES DIRETAS:
            # Substitui "Brazilian Dual-Audio" ou "Dual-Audio" por "BRAZILIAN-DUAL-AUDIO"
            - name: re_replace
              args: ['(?i)(brazilian\s+)?dual-audio', 'BRAZILIAN-DUAL-AUDIO']
            # Substitui "Brazilian Dublado" ou "Dublado" por "DUBLADO"
            - name: re_replace
              args: ['(?i)(brazilian\s+)?dublado', 'DUBLADO']
            # Substitui "Brazilian Nacional" ou "Nacional" por "NACIONAL"
            - name: re_replace
              args: ['(?i)(brazilian\s+)?nacional', 'NACIONAL']
            # Substitui "Brazilian Legendado" ou "Legendado" por "LEGENDADO"
            - name: re_replace
              args: ['(?i)(brazilian\s+)?legendado', 'LEGENDADO']
            - name: append
              args: "-ASC"
        details:
          selector: a[href*="torrents-details.php?id="], a[href*="details-misc.php?id="]
          attribute: href
        download:
          selector: a[href*="download.php?id="]
          attribute: href
        date:
          selector: p:contains("Lançado:")
          optional: true
          default: now
          filters:
            - name: regexp
              args: "Lançado: (.+?)$"
            - name: re_replace
              args: [" (\\d:)", " 0$1"]
            - name: dateparse
              args: "dd/MM/yy HH:mm:ss"
        size:
          selector: div.list-group-item-content p.m-0 span.badge-info
        seeders:
          selector: div.list-group-item-controls a:nth-child(1)
        leechers:
          selector: div.list-group-item-controls a:nth-child(2)
        grabs:
          selector: div.list-group-item-controls a:nth-child(3)
        genre:
          selector: div.list-group-item-content p.m-0 span.badge-primary[style$="#1c38c2;"]
        description:
          text: "{{ .Result.genre }}"
        downloadvolumefactor:
          case:
            "span.badge-success:contains(\"FREE\")": 0
            "*": 1
        uploadvolumefactor:
          text: 1
        minimumratio:
          text: 1.0
    # engine n/a
  bjshare-trashguides-ptbr.yml: |-
    ---
    id: bjshare-trashguides-pt-br
    name: Bj-Share TrashGuides PT-BR
    description: "Private PT-BR torrent"
    language: pt-BR
    type: private
    encoding: UTF-8
    links:
      - https://bj-share.info/

    caps:
      categorymappings:
        - {id: 1, cat: Movies, desc: "Filmes"}
        - {id: 2, cat: TV, desc: "TV"}
        - {id: 3, cat: PC, desc: "Aplicativos"}
        - {id: 4, cat: PC/Games, desc: "Jogos"}
        - {id: 5, cat: Books/Comics, desc: "Mangás"}
        - {id: 6, cat: TV, desc: "Vídeos de TV"}
        - {id: 7, cat: Other, desc: "Outros"}
        - {id: 8, cat: TV/Sport, desc: "Esportes"}
        - {id: 9, cat: Books/Mags, desc: "Revistas"}
        - {id: 10, cat: Books, desc: "E-Books"}
        - {id: 11, cat: Audio/Audiobook, desc: "Audiobook"}
        - {id: 12, cat: Books/Comics, desc: "Histórias em Quadrinhos"}
        - {id: 13, cat: TV, desc: "Stand Up Comedy"}
        - {id: 14, cat: TV, desc: "TV/Anime"}
        - {id: 15, cat: XXX/ImageSet, desc: "Fotos Adultas"}
        - {id: 16, cat: TV/Other, desc: "Desenho Animado"}
        - {id: 17, cat: TV/Documentary, desc: "Documentários"}
        - {id: 18, cat: Other, desc: "Cursos"}
        - {id: 19, cat: XXX, desc: "Filmes Adultos"}
        - {id: 20, cat: XXX/Other, desc: "Jogos Adultos"}
        - {id: 21, cat: XXX/Other, desc: "Mangás Adultos"}
        - {id: 22, cat: XXX/Other, desc: "Animes Adultos"}
        - {id: 23, cat: XXX/Other, desc: "HQ Adultos"}

      modes:
        search: [q]
        tv-search: [q, season, ep]
        movie-search: [q]

    settings:
      - name: cookie
        type: text
        label: Cookie
      - name: info_cookie
        type: info_cookie
      - name: freeleech
        type: checkbox
        label: Search freeleech only
        default: false

    login:
      method: cookie
      inputs:
        cookie: "{{ .Config.cookie }}"
      test:
        path: index.php
        selector: a[href^="/logout.php?auth="]

    search:
      paths:
        - path: torrents.php

      inputs:
        $raw: "{{ range .Categories }}filter_cat[{{.}}]=1&{{end}}"
        searchstr: "{{ .Keywords }}"
        freetorrent: "{{ if .Config.freeleech }}1{{ else }}{{ end }}"

      keywordsfilters:
        - name: re_replace
          args: ["(S[0-9]+E[0-9]+|S[0-9]+)", ""]

      rows:
        selector: "table.torrent_table > tbody > tr:not(tr.colhead).group,
                  table.torrent_table tbody tr:not(tr.colhead):contains('{{ .Query.Episode }}')"
        filters:
          - name: andmatch

      fields:
        download:
          selector: a[title="Baixar"]
          attribute: href

        _singleTorrentInfo:
          selector: div.torrent_info

        _multipleTorrentInfo:
          selector: a[href^="torrents.php?id="]

        _info:
          text: "{{ if .Result._singleTorrentInfo }}{{.Result._singleTorrentInfo}}{{ else }}{{.Result._multipleTorrentInfo}}{{ end }}"
          filters:
            - name: replace
              args: ["Full HD", "1080p"]
            - name: replace
              args: ["4K", "2160p"]
            - name: replace
              args: ["SD", "480p"]
            - name: replace
              args: ["/ HD]", "720p"]
            - name: replace
              args: ["/ HD /", "720p"]
            - name: replace
              args: ["/ Free", ""]
            - name: re_replace
              args: ["[\\[\\]]+", ""]

        _rawTitle:
          selector: div.group_info
          filters:
            - name: re_replace
              args: ["(\n.*)", ""]
            - name: re_replace
              args: ["  |\t", ""]

        _year:
          text: "{{ .Result._rawTitle }}"
          filters:
            - name: regexp
              args: "\\[([0-9]*)\\]"

        details:
          selector: a[href^="torrents.php?id="]:not(.tooltip)
          attribute: href

        title_MovieTV:
          text: "{{ .Result._rawTitle }}"
          filters:
            - name: re_replace
              args: ["\\[[0-9]*\\].*", ""]
            - name: re_replace
              args: ["(.*)\\[(!?[^/]*?)\\]", "$2"]
            - name: append
              args: "{{ .Result._year }}"
            - name: append
              args: " {{ .Result._info }}"
            # Substitui "Dual Ãudio" por "BRAZILIAN-DUAL-AUDIO"
            - name: re_replace
              args: ['Dual\s+Ã[\x80-\xFF]udio', 'BRAZILIAN-DUAL-AUDIO']
            - name: re_replace
              args: ['Dual\s+.udio', 'BRAZILIAN-DUAL-AUDIO']
            # Substitui "Legendado" por "LEGENDADO"
            - name: re_replace
              args: ['(?i)Legendado', 'LEGENDADO']
            # Substitui "Nacional" por "NACIONAL"
            - name: re_replace
              args: ['(?i)Nacional', 'NACIONAL']
            # Remove espaços múltiplos
            - name: re_replace
              args: ['\s{2,}', ' ']
            # Remove espaço antes do hífen do release group
            - name: re_replace
              args: ['\s+-BJSHARE', '-BJSHARE']
            - name: append
              args: "-BJSHARE"

        title_Other:
          text: "{{ .Result._rawTitle }}"
          filters:
            - name: re_replace
              args: ["\\[|\\]", " "]
            # Remove espaços múltiplos
            - name: re_replace
              args: ['\s{2,}', ' ']
            - name: append
              args: "-BJSHARE"

        title_details:
          text: "{{ .Result._rawTitle }}"
          filters:
            - name: re_replace
              args: ["\\[[0-9]*\\].*", ""]
            - name: re_replace
              args: ["(.*)\\[(!?[^/]*?)\\]", "$2"]

        category_details:
          selector: td.cats_col > a
          attribute: href
          filters:
            - name: regexp
              args: "%5b(\\d+?)%5d"

        category:
          text: "{{ .Result.category_details }}"

        title:
          text: "{{ if
                        or (eq .Result.category_details \"1\")
                       (or (eq .Result.category_details \"2\")  (or (eq .Result.category_details \"14\")
                       (or (eq .Result.category_details \"16\") (or (eq .Result.category_details \"17\")
                       (or eq .Result.category_details \"8\")
                       (or (eq .Result.category_details \"13\") (eq .Result.category_details \"6\")))))))
                 }}{{.Result.title_MovieTV}}{{ else }}{{.Result.title_Other}}{{ end }}"

        size:
          selector: td:nth-last-child(4)
        grabs:
          selector: td:nth-last-child(3)
        seeders:
          selector: td:nth-last-child(2)
        leechers:
          selector: td:nth-last-child(1)
        downloadvolumefactor:
          case:
            strong[title*="Free"]: 0
            "*": 1
        uploadvolumefactor:
          text: 1
        minimumratio:
          text: 1.0
        minimumseedtime:
          text: 604800
  brasiltracker-trashguides-ptbr.yml: "---\nid: brasiltracker-trashguides-ptbr\nname:
    BrasilTracker TrashGuides PT-BR\ndescription: \"BrasilTracker is a BRAZILIAN Private
    Torrent Tracker for MOVIES / TV / GENERAL\"\nlanguage: pt-BR\nencoding: UTF-8\ntype:
    private\nlinks:\n  - https://brasiltracker.org/\n\ncaps:\n  categorymappings:\n
    \   - {id: 16, cat: Audio/Audiobook, desc: \"Audiobooks\"}\n    - {id: 6, cat:
    TV/Anime, desc: \"Animes\"}\n    - {id: 11, cat: PC/0day, desc: \"Aplicativos\"}\n
    \   - {id: 15, cat: Other, desc: \"Cursos\"}\n    - {id: 8, cat: TV/Documentary,
    desc: \"Documentários\"}\n    - {id: 14, cat: TV/Sport, desc: \"Esportes\"}\n
    \   - {id: 3, cat: XXX, desc: \"Filmes XXX\"}\n    - {id: 1, cat: Movies, desc:
    \"Filmes\"}\n    - {id: 12, cat: Books/Comics, desc: \"Histórias em Quadrinhos\"}\n
    \   - {id: 9, cat: PC/Games, desc: \"Jogos\"}\n    - {id: 13, cat: Books/EBook,
    desc: \"Livros\"}\n    - {id: 10, cat: Books/Mags, desc: \"Revistas\"}\n    -
    {id: 2, cat: TV, desc: \"Séries\"}\n    - {id: 5, cat: Audio/Video, desc: \"Show\"}\n
    \   - {id: 7, cat: TV, desc: \"Televisão\"}\n\n  modes:\n    search: [q]\n    tv-search:
    [q, season, ep]\n    movie-search: [q, imdbid]\n\nsettings:\n  - name: cookie\n
    \   type: text\n    label: Cookie\n  - name: info_cookie\n    type: info_cookie\n
    \ - name: freeleech\n    type: checkbox\n    label: Search freeleech only\n    default:
    false\n  - name: sort\n    type: select\n    label: Sort requested from site\n
    \   default: time\n    options:\n      time: created\n      seeders: seeders\n
    \     size: size\n  - name: type\n    type: select\n    label: Order requested
    from site\n    default: desc\n    options:\n      desc: desc\n      asc: asc\n\nlogin:\n
    \ method: cookie\n  inputs:\n    cookie: \"{{ .Config.cookie }}\"\n  test:\n    path:
    index.php\n    selector: a[href^=\"logout.php?auth=\"]\n\nsearch:\n  paths:\n
    \   - path: torrents.php\n  inputs:\n    $raw: \"{{ range .Categories }}filter_cat[{{.}}]=1&{{end}}\"\n
    \   searchstr: \"{{ if .Query.IMDBID }}{{ .Query.IMDBID }}{{ else }}{{ .Keywords
    }}{{ end }}\"\n    order_by: \"{{ .Config.sort }}\"\n    order_way: \"{{ .Config.type
    }}\"\n    group_results: 0\n    action: basic\n    freetorrent: \"{{ if .Config.freeleech
    }}1{{ else }}{{ end }}\"\n    searchsubmit: 1\n\n  rows:\n    selector: table#torrent_table
    > tbody > tr.torrent\n\n  fields:\n    category:\n      selector: td.big_info
    div.group_info\n      case:\n        \"div:contains(\\\"[Audiobooks]\\\")\": 16\n
    \       \"div:contains(\\\"[Animes]\\\")\": 6\n        \"div:contains(\\\"[Aplicativos]\\\")\":
    11\n        \"div:contains(\\\"[Cursos]\\\")\": 15\n        \"div:contains(\\\"[Documentários]\\\")\":
    8\n        \"div:contains(\\\"[Esportes]\\\")\": 14\n        \"div:contains(\\\"[Filmes
    XXX]\\\")\": 3\n        \"div:contains(\\\"[Filmes]\\\")\": 1\n        \"div:contains(\\\"[Histórias
    em Quadrinhos]\\\")\": 12\n        \"div:contains(\\\"[Jogos]\\\")\": 9\n        \"div:contains(\\\"[Livros]\\\")\":
    13\n        \"div:contains(\\\"[Revistas]\\\")\": 10\n        \"div:contains(\\\"[Séries]\\\")\":
    2\n        \"div:contains(\\\"[Show]\\\")\": 5\n        \"div:contains(\\\"[Televisão]\\\")\":
    7\n        \"*\": 0\n    details:\n      selector: a[href^=\"torrents.php?id=\"]\n
    \     attribute: href\n    download:\n      selector: a[href^=\"torrents.php?action=download&id=\"]\n
    \     attribute: href\n    description:\n      selector: div.tags\n    poster:\n
    \     selector: img[alt=\"Cover\"]\n      attribute: src\n    imdbid:\n      selector:
    a[href*=\"imdb.com/title/tt\"]\n      attribute: href\n    tmdbid:\n      selector:
    a[href*=\"themoviedb.org/movie/\"], a[href*=\"themoviedb.org/tv/\"]\n      attribute:
    href\n    files:\n      selector: td:nth-child(3)\n    date:\n      selector:
    span.time\n      attribute: title\n      filters:\n        - name: append\n          args:
    \" -03:00\"\n        - name: dateparse\n          args: \"MMM d yyyy, HH:mm zzz\"\n
    \   size:\n      selector: td:nth-child(5)\n    grabs:\n      selector: td:nth-child(6)\n
    \   seeders:\n      selector: td:nth-child(7)\n    leechers:\n      selector:
    td:nth-child(8)\n    downloadvolumefactor:\n      case:\n        strong.tl_free:
    0\n        \"*\": 1\n    uploadvolumefactor:\n      text: 1\n    title_details:\n
    \     selector: div.torrent_info\n      remove: strong\n    year:\n      selector:
    td.big_info div.group_info\n      optional: true\n      filters:\n        - name:
    regexp\n          args: \"\\\\[((?:19|20)\\\\d{2})\\\\]\"\n    title_base:\n      selector:
    a[href^=\"torrents.php?id=\"]\n    title:\n      text: \"{{ .Result.title_base
    }}{{ if .Result.year }} {{ .Result.year }}{{ else }}{{ end }} {{ .Result.title_details
    }}\"\n      filters:\n        # Remove colchetes e conteúdo\n        - name: re_replace\n
    \         args: [\"\\\\[([^\\\\]]+)\\\\]\", \"\"]\n        # Remove barras extras\n
    \       - name: re_replace\n          args: [\" / \", \" \"]\n        # Padroniza
    WEB para WEB-DL\n        - name: re_replace\n          args: [\"\\\\bWEB\\\\b(?![-_.
    ]?DL)\", \"WEB-DL\"]\n        # Converte 4K para 2160p\n        - name: re_replace\n
    \         args: [\"(?i)\\\\b4K\\\\b\", \"2160p\"]\n        # Converte Full HD
    para 1080p\n        - name: re_replace\n          args: [\"(?i)\\\\bFull HD\\\\b\",
    \"1080p\"]\n        # Converte HD para 720p\n        - name: re_replace\n          args:
    [\"(?i)\\\\b(?<!Full )HD\\\\b\", \"720p\"]\n        # \U0001F525 NOVO: Converte
    \"Dual Audio\" para \"DUAL-BRAZILIAN-AUDIO\"\n        - name: re_replace\n          args:
    [\"(?i)Dual Audio\", \"BRAZILIAN-DUAL-AUDIO\"]\n        # \U0001F525 NOVO: Converte
    \"Subs\" ou \"Legendado\" para \"LEGENDADO\"\n        - name: re_replace\n          args:
    [\"(?i)\\\\b(Subs|Legendado)\\\\b\", \"LEGENDADO\"]\n        # Adiciona NACIONAL
    quando tem \"Nacional\"\n        - name: re_replace\n          args: [\"(?i)\\\\bNacional\\\\b\",
    \"NACIONAL\"]\n        # Adiciona DUBLADO quando tem \"Dublado\"\n        - name:
    re_replace\n          args: [\"(?i)\\\\bDublado\\\\b\", \"DUBLADO\"]\n        #
    Remove espaços múltiplos\n        - name: re_replace\n          args: [\"\\\\s+\",
    \" \"]\n        # Remove espaços antes de pontuação\n        - name: re_replace\n
    \         args: [\" \\\\.\", \".\"]\n        # Trim final\n        - name: trim\n
    \   minimumratio:\n      text: 1.0\n    minimumseedtime:\n      text: 172800\n"
  capybarabr-trashguides-ptbr.yml: |-
    ---
    id: capybarabr-trashguides-ptbr
    name: CapybaraBR TrashGuides PT-BR
    description: "CapybaraBR is a BRAZILIAN Private Torrent Tracker for MOVIES / TV / GENERAL"
    language: pt-BR
    type: private
    encoding: UTF-8
    links:
      - https://capybarabr.com/

    caps:
      categorymappings:
        - {id: 1, cat: Movies, desc: "Filmes"}
        - {id: 2, cat: TV, desc: "Series"}
        - {id: 4, cat: TV/Anime, desc: "Animes"}
        - {id: 8, cat: TV/Sport, desc: "Esportes"}
        - {id: 6, cat: Movies, desc: "Desenhos Animados"}
        - {id: 5, cat: Console, desc: "Jogos"}
        - {id: 9, cat: PC, desc: "Programas"}
        - {id: 10, cat: Books/Comics, desc: "HQs"}
        - {id: 11, cat: Books/EBook, desc: "Livros"}
        - {id: 12, cat: Other, desc: "Cursos"}
        - {id: 13, cat: Books/Mags, desc: "Revistas"}

      modes:
        search: [q]
        tv-search: [q, season, ep, imdbid, tvdbid, tmdbid]
        movie-search: [q, imdbid, tmdbid]
        music-search: [q]
        book-search: [q]

    settings:
      - name: apikey
        type: text
        label: APIKey
      - name: info_key
        type: info
        label: About your API key
        default: "Find or Generate a new API Token by accessing your <a href=\"https://capybarabr.com/\" target=\"_blank\">CapybaraBR</a> account <i>My Settings</i> page and clicking on the <b>API Key</b> tab."
      - name: freeleech
        type: checkbox
        label: Search freeleech only
        default: false
      - name: use_single_file_release_use_filename
        type: checkbox
        label: Use filename as the title for single file releases
        default: false
      - name: sort
        type: select
        label: Sort requested from site
        default: created_at
        options:
          created_at: created
          seeders: seeders
          size: size
          name: title
      - name: type
        type: select
        label: Order requested from site
        default: desc
        options:
          desc: desc
          asc: asc
      - name: info_activity
        type: info
        label: Account Inactivity
        default: "The system automatically makes an account inactive when not logged in for a period of 90 days. So just log in within this time so that it is not inactivated."

    login:
      path: /api/torrents
      method: get
      inputs: {}
      error:
        - selector: a[href*="/login"]
          message:
            text: "The API key was not accepted by {{ .Config.sitelink }}."
        - selector: :root:contains("Account is Banned")

    search:
      paths:
        - path: api/torrents/filter
          response:
            type: json

      headers:
        Authorization: ["Bearer {{ .Config.apikey }}"]

      inputs:
        $raw: "{{ range .Categories }}&categories[]={{.}}{{end}}"
        name: "{{ .Keywords }}"
        seasonNumber: "{{ .Query.Season }}"
        episodeNumber: "{{ .Query.Ep }}"
        imdbId: "{{ .Query.IMDBIDShort }}"
        tmdbId: "{{ .Query.TMDBID }}"
        tvdbId: "{{ .Query.TVDBID }}"
        "free[]": "{{ if .Config.freeleech }}100{{ else }}{{ end }}"
        sortField: "{{ .Config.sort }}"
        sortDirection: "{{ .Config.type }}"
        perPage: 100

      keywordsfilters:
        - name: re_replace
          args: ["\\.", " "]

      rows:
        selector: data
        attribute: attributes

      fields:
        category:
          selector: category_id
        title_optional:
          selector: name
        title_filename:
          selector: "files[0].name"
          optional: true
        files:
          selector: num_file
        title:
          text: "{{ if and (.Config.use_single_file_release_use_filename) (eq .Result.files \"1\") (.Result.title_filename) }}{{ .Result.title_filename }}{{ else }}{{ .Result.title_optional }}{{ end }}"
          filters:
            - name: re_replace
              args: ['(?i)\bDUAL\b', 'BRAZILIAN-DUAL-AUDIO']
        details:
          selector: details_link
        download:
          selector: download_link
        infohash:
          selector: info_hash
        poster:
          selector: meta.poster
          filters:
            - name: replace
              args: ["https://via.placeholder.com/90x135", ""]
        imdbid:
          selector: imdb_id
        tmdbid:
          selector: tmdb_id
        tvdbid:
          selector: tvdb_id
        genre:
          selector: meta.genres
          filters:
            - name: re_replace
              args: ["(?i)(Ficção científica)", "Ficção_científica"]
            - name: re_replace
              args: ["(?i)(Cinema TV)", "Cinema_TV"]
            - name: replace
              args: [" & ", "_&_"]
        _internal:
          selector: internal
          case:
            False: "{{ .False }}"
            True: "{{ .True }}"
        description:
          text: "{{ if .Result._internal }}Internal{{ else }}{{ end }}{{ if and .Result._internal .Result.genre }} | {{ else }}{{ end }}{{ .Result.genre }}"
        seeders:
          selector: seeders
        leechers:
          selector: leechers
        grabs:
          selector: times_completed
        date:
          selector: created_at
          filters:
            - name: append
              args: " -03:00"
            - name: dateparse
              args: "MM/dd/yyyy HH:mm:ss zzz"
        size:
          selector: size
        _featured:
          selector: featured
          case:
            False: "{{ .False }}"
            True: "{{ .True }}"
        downloadvolumefactor_freeleech:
          selector: freeleech
          case:
            0%: 1
            25%: 0.75
            50%: 0.5
            75%: 0.25
            100%: 0
            "*": 0
        downloadvolumefactor:
          text: "{{ if .Result._featured }}0{{ else }}{{ .Result.downloadvolumefactor_freeleech }}{{ end }}"
        uploadvolumefactor_double_upload:
          selector: double_upload
          case:
            False: 1
            True: 2
        uploadvolumefactor:
          text: "{{ if .Result._featured }}2{{ else }}{{ .Result.uploadvolumefactor_double_upload }}{{ end }}"
        minimumseedtime:
          text: 604800
  locadora-trashguides-ptbr.yml: |-
    ---
    id: locadora-trashguides-pt-br
    name: Locadora (API) TrashGuides PT-BR
    description: "Locadora is a BRAZILIAN Private Torrent Tracker for MOVIES / TV / ANIME"
    language: pt-BR
    type: private
    encoding: UTF-8
    links:
      - https://locadora.cc/
    legacylinks:
      - https://locadora.xyz/

    caps:
      categorymappings:
        - {id: 1, cat: Movies, desc: "Filmes"}
        - {id: 2, cat: TV, desc: "Series"}
        - {id: 6, cat: TV/Anime, desc: "Animes"}
        - {id: 7, cat: Audio/Video, desc: "Shows"}

      modes:
        search: [q]
        tv-search: [q, season, ep, imdbid, tvdbid, tmdbid]
        movie-search: [q, imdbid, tmdbid]
        music-search: [q]

    settings:
      - name: apikey
        type: text
        label: APIKey
      - name: info_key
        type: info
        label: About your API key
        default: "Find or Generate a new API Token by accessing your <a href=\"https://locadora.cc/\" target=\"_blank\">Locadora</a> account <i>My Settings</i> page and clicking on the <b>API Key</b> tab."
      - name: freeleech
        type: checkbox
        label: Search freeleech only
        default: false
      - name: single_file_release_use_filename
        type: checkbox
        label: Use filename as title for single file releases
        default: false
      - name: sort
        type: select
        label: Sort requested from site
        default: created_at
        options:
          created_at: created
          seeders: seeders
          size: size
          name: title
      - name: type
        type: select
        label: Order requested from site
        default: desc
        options:
          desc: desc
          asc: asc
      - name: info_activity
        type: info
        label: Account Inactivity
        default: "Accounts that are inactive for 90 days are deactivated."

    login:
      path: /api/torrents
      method: get
      inputs: {}
      error:
        - selector: a[href*="/login"]
          message:
            text: "The API key was not accepted by {{ .Config.sitelink }}."
        - selector: :root:contains("Account is Banned")

    search:
      paths:
        - path: api/torrents/filter
          response:
            type: json

      headers:
        Authorization: ["Bearer {{ .Config.apikey }}"]

      inputs:
        $raw: "{{ range .Categories }}&categories[]={{.}}{{end}}"
        name: "{{ .Keywords }}"
        seasonNumber: "{{ .Query.Season }}"
        episodeNumber: "{{ .Query.Ep }}"
        imdbId: "{{ .Query.IMDBIDShort }}"
        tmdbId: "{{ .Query.TMDBID }}"
        tvdbId: "{{ .Query.TVDBID }}"
        "free[]": "{{ if .Config.freeleech }}100{{ else }}{{ end }}"
        sortField: "{{ .Config.sort }}"
        sortDirection: "{{ .Config.type }}"
        perPage: 100

      keywordsfilters:
        - name: re_replace
          args: ["\\.", " "]

      rows:
        selector: data
        attribute: attributes

      fields:
        category:
          selector: category_id
        title_optional:
          selector: name
        title_filename:
          selector: "files[0].name"
          optional: true
        files:
          selector: num_file
        title:
          text: "{{ if and (.Config.single_file_release_use_filename) (eq .Result.files \"1\") (.Result.title_filename) }}{{ .Result.title_filename }}{{ else }}{{ .Result.title_optional }}{{ end }}"
          filters:
            # 1. Substitui "DUAL-" por "BRAZILIAN-DUAL-AUDIO-"
            - name: re_replace
              args: ['(?i)\bDUAL-', 'BRAZILIAN-DUAL-AUDIO-']
            # 2. Se tem Portuguese + outro idioma, só remove o sufixo (já tem BRAZILIAN-DUAL-AUDIO)
            - name: re_replace
              args: ['(?i)\s*/\s*Portuguese\s*\([^)]*\)\s+\w+.*$', '']
            # 3. Se tem GLBO + só Portuguese, adiciona NACIONAL antes do release group
            #- name: re_replace
            #  args: ['(?i)^(.+GLBO.+?)(\S+)\s*/\s*Portuguese\s*\([^)]*\)\s*$', '$1NACIONAL-$2']
            # 4. Se tem só Portuguese (sem GLBO), adiciona DUBLADO antes do release group
            - name: re_replace
              args: ['(?i)(\S+)\s*/\s*Portuguese\s*\([^)]*\)\s*$', 'NACIONAL-$1']
            # 5. Se tem qualquer outro idioma (sem Portuguese), adiciona LEGENDADO
            - name: re_replace
              args: ['(?i)(\S+)\s*/\s*(?!Portuguese)[A-Za-z].*$', 'LEGENDADO-$1']
            # 6. Remove "-JPN" ou "-jpn" do título
            - name: re_replace
              args: ['(?i)-JPN', '']
        details:
          selector: details_link
        download:
          selector: download_link
        infohash:
          selector: info_hash
        poster:
          selector: meta.poster
          filters:
            - name: replace
              args: ["https://via.placeholder.com/90x135", ""]
        imdbid:
          selector: imdb_id
        tmdbid:
          selector: tmdb_id
        tvdbid:
          selector: tvdb_id
        genre:
          selector: meta.genres
          filters:
            - name: re_replace
              args: ["(?i)(Cinema TV)", "Cinema_TV"]
            - name: re_replace
              args: ["(?i)(Ficção científica)", "Ficção_científica"]
            - name: replace
              args: [" & ", "_&_"]
        _internal:
          selector: internal
          case:
            False: "{{ .False }}"
            True: "{{ .True }}"
        description:
          text: "{{ if .Result._internal }}Internal{{ else }}{{ end }}{{ if and .Result._internal .Result.genre }} | {{ else }}{{ end }}{{ .Result.genre }}"
        seeders:
          selector: seeders
        leechers:
          selector: leechers
        grabs:
          selector: times_completed
        date:
          selector: created_at
          filters:
            - name: append
              args: " +00:00"
            - name: dateparse
              args: "MM/dd/yyyy HH:mm:ss zzz"
        size:
          selector: size
        _featured:
          selector: featured
          case:
            False: "{{ .False }}"
            True: "{{ .True }}"
        downloadvolumefactor_freeleech:
          selector: freeleech
          case:
            0%: 1
            25%: 0.75
            50%: 0.5
            75%: 0.25
            100%: 0
            "*": 0
        downloadvolumefactor:
          text: "{{ if .Result._featured }}0{{ else }}{{ .Result.downloadvolumefactor_freeleech }}{{ end }}"
        uploadvolumefactor_double_upload:
          selector: double_upload
          case:
            False: 1
            True: 2
        uploadvolumefactor:
          text: "{{ if .Result._featured }}2{{ else }}{{ .Result.uploadvolumefactor_double_upload }}{{ end }}"
        minimumseedtime:
          text: 259200
  samaritano-trashguides-ptbr.yml: |-
    ---
    id: samaritano-trashguides-ptbr
    name: SAMARITANO TrashGuides PT-BR
    description: "SAMARITANO is a BRAZILIAN Private tracker for MOVIES / TV / GENERAL"
    language: pt-BR
    type: private
    encoding: UTF-8
    links:
      - https://samaritano.cc/

    caps:
      categorymappings:
        - {id: 1, cat: Movies, desc: "Filmes"}
        - {id: 2, cat: TV, desc: "Séries"}
        - {id: 3, cat: TV/Anime, desc: "Animes"}
        - {id: 4, cat: Other, desc: "Cursos"}
        - {id: 5, cat: Console, desc: "Games"}
        - {id: 6, cat: Books/EBook, desc: "Livros"}
        - {id: 7, cat: Books/Comics, desc: "HQs"}
        - {id: 8, cat: Audio/Audiobook, desc: "AudioBooks"}
        - {id: 9, cat: PC, desc: "Programas"}
        - {id: 11, cat: Other, desc: "Diversos"}
        - {id: 10, cat: Other, desc: "Materiais de Apoio"}

      modes:
        search: [q]
        tv-search: [q, season, ep, imdbid, tvdbid, tmdbid]
        movie-search: [q, imdbid, tmdbid]
        music-search: [q]
        book-search: [q]

    settings:
      - name: apikey
        type: text
        label: APIKey
      - name: info_key
        type: info
        label: About your API key
        default: "Find or Generate a new API Token by accessing your <a href=\"https://samaritano.cc/\" target=\"_blank\">SAMARITANO</a> account <i>My Settings</i> page and clicking on the <b>API Key</b> tab."
      - name: freeleech
        type: checkbox
        label: Search freeleech only
        default: false
      - name: single_file_release_use_filename
        type: checkbox
        label: Use filename as title for single file releases
        default: false
      - name: sort
        type: select
        label: Sort requested from site
        default: created_at
        options:
          created_at: created
          seeders: seeders
          size: size
          name: title
      - name: type
        type: select
        label: Order requested from site
        default: desc
        options:
          desc: desc
          asc: asc
      - name: info_activity
        type: info
        label: Account Inactivity
        default: "Your account will be considered inactive if you have not logged in to the site for more than 90 days. After 30 days of inactivity (i.e. 30 days without logging in), your account will be removed from the tracker. Please note: The groups that suffer from inactivity are: User and Leech."

    login:
      path: /api/torrents
      method: get
      error:
        - selector: a[href*="/login"]
          message:
            text: "The API key was not accepted by {{ .Config.sitelink }}."
        - selector: :root:contains("Account is Banned")

    search:
      paths:
        - path: api/torrents/filter
          response:
            type: json

      headers:
        Authorization: ["Bearer {{ .Config.apikey }}"]

      inputs:
        $raw: "{{ range .Categories }}&categories[]={{.}}{{end}}"
        name: "{{ .Keywords }}"
        seasonNumber: "{{ .Query.Season }}"
        episodeNumber: "{{ .Query.Ep }}"
        imdbId: "{{ .Query.IMDBIDShort }}"
        tmdbId: "{{ .Query.TMDBID }}"
        tvdbId: "{{ .Query.TVDBID }}"
        "free[]": "{{ if .Config.freeleech }}100{{ else }}{{ end }}"
        sortField: "{{ .Config.sort }}"
        sortDirection: "{{ .Config.type }}"
        perPage: 100

      keywordsfilters:
        - name: re_replace
          args: ["\\.", " "]

      rows:
        selector: data
        attribute: attributes

      fields:
        category:
          selector: category_id
        title_optional:
          selector: name
        title_filename:
          selector: "files[0].name"
          optional: true
        files:
          selector: num_file
        title:
          text: "{{ if and (.Config.single_file_release_use_filename) (eq .Result.files \"1\") (.Result.title_filename) }}{{ .Result.title_filename }}{{ else }}{{ .Result.title_optional }}{{ end }}"
          filters:
            - name: re_replace
              args: ['(?i)\bDUAL\b', 'BRAZILIAN-DUAL-AUDIO']
        details:
          selector: details_link
        download:
          selector: download_link
        poster:
          selector: meta.poster
          filters:
            - name: replace
              args: ["https://via.placeholder.com/90x135", ""]
        imdbid:
          selector: imdb_id
        tmdbid:
          selector: tmdb_id
        tvdbid:
          selector: tvdb_id
        genre:
          selector: meta.genres
          filters:
            - name: re_replace
              args: ["(?i)(Cinema TV)", "Cinema_TV"]
            - name: re_replace
              args: ["(?i)(Ficção científica)", "Ficção_científica"]
            - name: replace
              args: [" & ", "_&_"]
        _internal:
          selector: internal
          case:
            False: "{{ .False }}"
            True: "{{ .True }}"
        description:
          text: "{{ if .Result._internal }}Internal{{ else }}{{ end }}{{ if and .Result._internal .Result.genre }} | {{ else }}{{ end }}{{ .Result.genre }}"
        seeders:
          selector: seeders
        leechers:
          selector: leechers
        grabs:
          selector: times_completed
        date:
          selector: created_at
          filters:
            - name: append
              args: " +00:00"
            - name: dateparse
              args: "MM/dd/yyyy HH:mm:ss zzz"
        size:
          selector: size
        _featured:
          selector: featured
          case:
            False: "{{ .False }}"
            True: "{{ .True }}"
        downloadvolumefactor_freeleech:
          selector: freeleech
          case:
            0%: 1
            25%: 0.75
            50%: 0.5
            75%: 0.25
            100%: 0
            "*": 0
        downloadvolumefactor:
          text: "{{ if .Result._featured }}0{{ else }}{{ .Result.downloadvolumefactor_freeleech }}{{ end }}"
        uploadvolumefactor_double_upload:
          selector: double_upload
          case:
            False: 1
            True: 2
        uploadvolumefactor:
          text: "{{ if .Result._featured }}2{{ else }}{{ .Result.uploadvolumefactor_double_upload }}{{ end }}"
        minimumseedtime:
          text: 604800
  shakaw-trashguides-ptbr.yml: |-
    ---
    id: shakaw-trashguides-ptbr
    name: ShaKaw TrashGuides PT-BR
    description: "ShaKaw is a BRAZILIAN Private Torrent Tracker for ANIME"
    language: pt-BR
    type: private
    encoding: UTF-8
    requestDelay: 2
    links:
      - https://tracker.shakaw.com.br/
    caps:
      categorymappings:
        - {id: 0, cat: TV/Anime, desc: "Selecione"}
        - {id: 1, cat: TV/Anime, desc: "Completo"}
        - {id: 2, cat: TV/Anime, desc: "Série"}
        - {id: 3, cat: Movies, desc: "Filme"}
        - {id: 4, cat: TV/Anime, desc: "OVA"}
        - {id: 5, cat: Books, desc: "Manga"}
        - {id: 6, cat: TV/Anime, desc: "Especial"}
        - {id: 7, cat: TV/Anime, desc: "Promo"}
        - {id: 8, cat: TV, desc: "Tokusatsu"}
        - {id: 9, cat: TV, desc: "Live Action"}
        - {id: 12, cat: TV, desc: "Dorama"}
      modes:
        search: [q]
        tv-search: [q, season, ep]
        movie-search: [q]
        book-search: [q]
    settings:
      - name: username
        type: text
        label: Username
      - name: password
        type: password
        label: Password
      - name: freeleech
        type: checkbox
        label: Search freeleech only
        default: false
    login:
      method: form
      path: entrar.php
      submitpath: pagina_inicial.php?acao=login
      form: form[action="pagina_inicial.php?acao=login"]
      captcha:
        type: image
        selector: img[alt="Carregando captcha..."]
        input: captcha
      inputs:
        username: "{{ .Config.username }}"
        password: "{{ .Config.password }}"
      error:
        - selector: h1#h1_aviso
          message:
            selector: div#div_aviso > p
      test:
        path: pagina_inicial.php
        selector: a[href="sair.php"]
    search:
      paths:
        - path: torrents.php
      inputs:
        palavra_chave: "{{ .Keywords }}"
        # selecione (default), Com seeders (has seeders), Sem seeders (without seeders), Golden, Sem Golden (not Golden)
        situacao: "{{ if .Config.freeleech }}golden{{ else }}selecione{{ end }}"
      rows:
        selector: table#tabela_torrents > tbody > tr
      fields:
        category:
          selector: div.div_categoria_torrent > a
          attribute: href
          filters:
            - name: querystring
              args: categoria_id
        title:
          selector: a.link_torrent, a.link_golden_torrent
          filters:
            # Adiciona -SHAKAW no final
            - name: append
              args: "-SHAKAW"
        details:
          selector: a.link_torrent, a.link_golden_torrent
          attribute: href
        download:
          selector: a.link_download
          attribute: href
        date:
          selector: td:nth-child(4)
          filters:
            - name: trim
            - name: replace
              args: ["h", ":"]
            - name: re_replace
              args: ["^(.{5})", "$1 "]
            - name: append
              args: " -03:00" # America/Sao_Paulo
            - name: dateparse
              args: "HH:mm dd/MM/yyyy zzz"
        size:
          selector: td[title="Tamanho"]
        files:
          selector: td[title="Quantidade de Arquivos"] > a
        grabs:
          selector: td[title="Quantos Completaram"]
        seeders:
          selector: td[title="Quantidade de Seeders"] > a
        leechers:
          selector: td[title="Quantidade de Leechers"] > a
        genre:
          selector: div.tags_da_linha_da_tabela
          filters:
            - name: regexp
              args: "\\[(.+?)\\]"
        description:
          selector: td:nth-child(10)
        downloadvolumefactor:
          case:
            span.icone_golden_torrent: 0
            "*": 1
        uploadvolumefactor:
          text: 1
        minimumratio:
          text: 0.6
kind: ConfigMap
metadata:
  name: prowlarr-custom-definitions
