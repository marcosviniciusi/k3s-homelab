apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: whisparr-infisical-secret
spec:
  resyncInterval: 60
  hostAPI: "https://cofre.vinicima.com/api"

  authentication:
    serviceToken:
      serviceTokenSecretReference:
        secretName: whisparr-service-token
        secretNamespace: arr
      secretsScope:
        envSlug: production
        secretsPath: /whisparr

  managedKubeSecretReferences:
    - secretName: whisparr-secrets
      secretNamespace: arr
      secretType: Opaque
      template:
        includeAllSecrets: true



