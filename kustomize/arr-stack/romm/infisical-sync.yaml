apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: romm-infisical-secret
spec:
  resyncInterval: 60
  hostAPI: "https://cofre.vinicima.com/api"

  authentication:
    serviceToken:
      serviceTokenSecretReference:
        secretName: romm-service-token
        secretNamespace: arr
      secretsScope:
        envSlug: production
        secretsPath: /romm

  managedKubeSecretReferences:
    - secretName: romm-secrets
      secretNamespace: arr
      secretType: Opaque
      template:
        includeAllSecrets: true



