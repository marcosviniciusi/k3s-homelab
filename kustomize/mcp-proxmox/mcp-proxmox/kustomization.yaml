apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mcp-proxmox

resources:
  - namespace.yaml
  - serviceaccount.yaml
  # Adicione seus SealedSecrets aqui:
  # - sealedsecret-config.yaml
  # - sealedsecret-ssh.yaml
  - deployment.yaml
  - service.yaml
