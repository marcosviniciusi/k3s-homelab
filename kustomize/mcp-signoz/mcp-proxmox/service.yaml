apiVersion: v1
kind: Service
metadata:
  name: mcp-signoz
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.253.42
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: mcp-signoz
